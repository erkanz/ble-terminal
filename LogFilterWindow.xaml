<Window x:Class="BLESerialTerminal.LogFilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Diagnostic Log Filters / Search" Height="780" Width="1360"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        MinHeight="620" MinWidth="1000"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Search:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox x:Name="SearchTextBox" Grid.Column="1" Margin="0,0,10,0" TextChanged="FilterControl_Changed"/>
                <TextBlock Grid.Column="2" Text="Category:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="CategoryComboBox" Grid.Column="3" SelectedIndex="0" SelectionChanged="FilterControl_Changed">
                    <ComboBoxItem Content="ALL"/>
                    <ComboBoxItem Content="CONNECTION"/>
                    <ComboBoxItem Content="DISCOVERY"/>
                    <ComboBoxItem Content="GATT"/>
                    <ComboBoxItem Content="CCCD"/>
                    <ComboBoxItem Content="RX_RAW"/>
                    <ComboBoxItem Content="TX_RAW"/>
                    <ComboBoxItem Content="KISS"/>
                    <ComboBoxItem Content="AX25"/>
                    <ComboBoxItem Content="APRS"/>
                    <ComboBoxItem Content="INSPECTOR"/>
                    <ComboBoxItem Content="WARNING"/>
                    <ComboBoxItem Content="ERROR"/>
                </ComboBox>
                <TextBlock Grid.Column="4" Text="Device:" VerticalAlignment="Center" Margin="10,0,6,0"/>
                <TextBox x:Name="DeviceFilterTextBox" Grid.Column="5" TextChanged="FilterControl_Changed"/>
                <TextBlock Grid.Column="6" Text="Characteristic:" VerticalAlignment="Center" Margin="10,0,6,0"/>
                <TextBox x:Name="CharacteristicFilterTextBox" Grid.Column="7" TextChanged="FilterControl_Changed" ToolTip="Short UUID such as FFE1 or full UUID"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Text="Direction:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox x:Name="DirectionComboBox" Width="90" SelectedIndex="0" SelectionChanged="FilterControl_Changed">
                        <ComboBoxItem Content="ALL"/>
                        <ComboBoxItem Content="RX"/>
                        <ComboBoxItem Content="TX"/>
                    </ComboBox>
                    <CheckBox x:Name="ErrorsWarningsOnlyCheckBox" Content="Errors / warnings only" Margin="12,0,0,0" VerticalAlignment="Center" Checked="FilterControl_Changed" Unchecked="FilterControl_Changed"/>
                    <CheckBox x:Name="AutoScrollCheckBox" Content="Auto scroll" Margin="12,0,0,0" VerticalAlignment="Center" IsChecked="True"/>
                    <Button Content="Clear filters" Width="100" Margin="12,0,0,0" Click="ClearFiltersButton_Click"/>
                    <Button Content="Previous match" Width="110" Margin="8,0,0,0" Click="PreviousMatchButton_Click"/>
                    <Button Content="Next match" Width="100" Margin="8,0,0,0" Click="NextMatchButton_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Content="Copy row" Width="90" Margin="0,0,8,0" Click="CopyRowButton_Click"/>
                    <Button Content="Export filtered" Width="110" Margin="0,0,8,0" Click="ExportFilteredButton_Click"/>
                    <Button Content="Export full" Width="100" Click="ExportFullButton_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <DataGrid x:Name="LogGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow" SelectionChanged="LogGrid_SelectionChanged"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True" Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Sequence}" Width="70"/>
                <DataGridTextColumn Header="Time" Binding="{Binding TimeText}" Width="105"/>
                <DataGridTextColumn Header="Category" Binding="{Binding CategoryText}" Width="105"/>
                <DataGridTextColumn Header="Level" Binding="{Binding LevelText}" Width="85"/>
                <DataGridTextColumn Header="Dir" Binding="{Binding Direction}" Width="55"/>
                <DataGridTextColumn Header="Device" Binding="{Binding Device}" Width="160"/>
                <DataGridTextColumn Header="Characteristic" Binding="{Binding Characteristic}" Width="150"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Selected entry" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="DetailTextBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas"/>
            </Grid>
        </Border>

        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock x:Name="ResultCountTextBlock" Text="0 results"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="Search filters the visible result set; Previous/Next selects and highlights the current match."/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
