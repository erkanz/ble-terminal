<Window x:Class="BLESerialTerminal.MultiDeviceCompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Multi-Device Live Compare" Width="1380" Height="860" MinWidth="1080" MinHeight="700"
        Icon="Assets/BLESerialTerminal.ico"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        WindowStartupLocation="CenterOwner"
        Closed="Window_Closed">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="190"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,8">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button x:Name="StartScanButton" Content="Start scan" Width="95" Margin="6,0,0,0" Click="StartScanButton_Click"/>
                    <Button x:Name="StopScanButton" Content="Stop scan" Width="95" Margin="6,0,0,0" Click="StopScanButton_Click" IsEnabled="False"/>
                    <Button Content="Close" Width="80" Margin="6,0,0,0" Click="CloseButton_Click"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Multi-Device Live Compare" FontWeight="SemiBold" FontSize="16"/>
                    <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource MutedForegroundBrush}" TextWrapping="Wrap"
                               Text="Each compared radio owns a separate BluetoothLEDevice, service/characteristic wrappers, GATT gate, TX queue, CCCD subscription, KISS decoder and counters. The main terminal connection is not reused here."/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="7" Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0" Margin="0,0,0,6">
                    <TextBlock Text="Discovered BLE devices" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="ScanFilterTextBox" Width="180" TextChanged="ScanFilterTextBox_TextChanged"/>
                        <Button Content="Connect selected" Width="120" Margin="8,0,0,0" Click="ConnectSelectedButton_Click"/>
                    </StackPanel>
                </DockPanel>
                <DataGrid x:Name="DiscoveryGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                          EnableRowVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="BLE Address" Binding="{Binding AddressText}" Width="170"/>
                        <DataGridTextColumn Header="RSSI" Binding="{Binding RssiText}" Width="95"/>
                        <DataGridTextColumn Header="Last seen" Binding="{Binding LastSeenText}" Width="105"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Compare A:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="CompareAComboBox" Grid.Column="1" DisplayMemberPath="Display" Margin="0,0,10,0"/>
                <TextBlock Grid.Column="2" Text="B:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="CompareBComboBox" Grid.Column="3" DisplayMemberPath="Display" Margin="0,0,10,0"/>
                <TextBlock Grid.Column="4" Text="KISS HEX TX:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="KissTxTextBox" Grid.Column="5" FontFamily="Consolas" VerticalContentAlignment="Center" ToolTip="Example: C0 00 ... C0"/>
                <Button Grid.Column="6" Content="Send to selected" Width="120" Margin="8,0,0,0" Click="SendSelectedButton_Click"/>
                <Button Grid.Column="7" Content="Disconnect selected" Width="130" Margin="8,0,0,0" Click="DisconnectSelectedButton_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="7" Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0" Margin="0,0,0,6">
                    <TextBlock Text="Independent live sessions (maximum 4)" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Content="Refresh comparison" Width="125" Click="RefreshComparisonButton_Click"/>
                        <Button Content="Disconnect all" Width="110" Margin="8,0,0,0" Click="DisconnectAllButton_Click"/>
                    </StackPanel>
                </DockPanel>
                <DataGrid x:Name="SessionGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                          EnableRowVirtualization="True" EnableColumnVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Device" Binding="{Binding DeviceName}" Width="150"/>
                        <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="135"/>
                        <DataGridTextColumn Header="State" Binding="{Binding State}" Width="110"/>
                        <DataGridTextColumn Header="Profile" Binding="{Binding Profile}" Width="170"/>
                        <DataGridTextColumn Header="Svc" Binding="{Binding ServiceUuid}" Width="75"/>
                        <DataGridTextColumn Header="Write" Binding="{Binding WriteUuid}" Width="75"/>
                        <DataGridTextColumn Header="Notify" Binding="{Binding NotifyUuid}" Width="75"/>
                        <DataGridTextColumn Header="CCCD" Binding="{Binding CccdText}" Width="60"/>
                        <DataGridTextColumn Header="Notif" Binding="{Binding Notifications}" Width="70"/>
                        <DataGridTextColumn Header="RX B" Binding="{Binding RxBytes}" Width="80"/>
                        <DataGridTextColumn Header="KISS" Binding="{Binding KissFrames}" Width="65"/>
                        <DataGridTextColumn Header="AX25" Binding="{Binding Ax25Frames}" Width="65"/>
                        <DataGridTextColumn Header="APRS" Binding="{Binding AprsPackets}" Width="65"/>
                        <DataGridTextColumn Header="TX B" Binding="{Binding TxBytes}" Width="80"/>
                        <DataGridTextColumn Header="Last packet" Binding="{Binding LastPacket}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="7" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="A / B comparison" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <DataGrid x:Name="ComparisonGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                              EnableRowVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Field" Binding="{Binding Field}" Width="150"/>
                            <DataGridTextColumn Header="A" Binding="{Binding A}" Width="*"/>
                            <DataGridTextColumn Header="" Binding="{Binding Marker}" Width="35"/>
                            <DataGridTextColumn Header="B" Binding="{Binding B}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
            <Border Grid.Column="1" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="7">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,6">
                        <TextBlock Text="Session diagnostics" FontWeight="SemiBold"/>
                        <Button DockPanel.Dock="Right" Content="Clear" Width="75" Click="ClearDiagnosticsButton_Click"/>
                    </DockPanel>
                    <TextBox x:Name="DiagnosticTextBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                             FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                             Background="{DynamicResource TerminalBackgroundBrush}" Foreground="{DynamicResource TerminalForegroundBrush}"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
